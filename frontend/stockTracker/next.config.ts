import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",
  distDir: "../../backend/stockTracker/src/main/resources/static",
};

export default nextConfig;
